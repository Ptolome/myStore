{
  "compilerOptions": {
    "target": "ES2020",            // Целевая версия JavaScript
    "module": "commonjs",       // Модульная система
    "outDir": "./dist",         // Директория для выходных файлов
    "rootDir": "./src",         // Корневая директория исходных файлов
    "strict": true,             // Включение строгого режима
    "esModuleInterop": true,      // Поддержка импорта ES6 модулей
    "skipLibCheck": true,       // Пропуск проверки типов для библиотек
    "forceConsistentCasingInFileNames": true,  // Принудительное сопоставление регистра в именах файлов
    "baseUrl": "./",             // Корневая директория для вызовов ESM
    "paths": {
      "@/*": ["src/*"]         // Пути для импортов ESM в директории src настрока алиасов
    },
  },
  "include": ["src/**/*"],      // Включение всех файлов в директории src
  "exclude": ["node_modules", "**/*.test.ts"] // Исключение всех файлов в директории node_modules и файлов с расширением .test.ts
}